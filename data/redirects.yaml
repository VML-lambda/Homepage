# 사이트 리다이렉트 규칙
# `from` 경로로 접속하면 `to` 로 이동합니다.
# 새 리다이렉트는 아래에 from/to 한 쌍을 추가하면 끝입니다. (그 외 작업 불필요)
# 주의: from 경로가 기존에 존재하지 않는 경로여야 함
#   - from: /짧은경로
#     to: /실제/페이지/경로/

- from: /professor
  to: /members/professor/
